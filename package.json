{
  "name": "nebula-worker",
  "private": true,
  "cloudflare": {
    "bindings": {
      "SESSION_SECRET": {
        "description": "（可选）不填也行。若需要自定义：用 `openssl rand -hex 32` 生成。"
      }
    }
  }
}
